import { Box, Typography, Card, CardContent } from '@mui/material';
export default function KPIsPage() {
  return (<Box><Typography variant="h4" sx={{ mb: 3 }}>KPIs</Typography><Card><CardContent><Typography>KPI management page. Backend ready!</Typography></CardContent></Card></Box>);
}
